/**
 * test for js doc
 * @param {string} nom 
 * @param {number} age 
 * @param {string} email 
 * @returns 
 */
 function test(nom, age, email){
    return `${nom} ${age} ans, son email : ${email}`;
}